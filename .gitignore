# Ambiente
.env
.env.local

# Dependências e build (Nest e Vite geram tudo isso)
node_modules/
dist/
build/

# Cache e artefatos temporários
*.tsbuildinfo
coverage/
*.log

# tsconfig.node.json (apps/web) não define outDir — "tsc -b" emite o JS
# compilado ao lado do vite.config.ts em vez de numa pasta de build.
apps/web/vite.config.js
apps/web/vite.config.d.ts

# Editor / SO
.vscode/*
!.vscode/extensions.json
.DS_Store
Thumbs.db
