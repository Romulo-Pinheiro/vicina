# URL base da API, para o services/ montar as chamadas fetch.
# Em dev, o vite.config.ts já faz proxy de /api para a API local (porta 3000),
# então isso normalmente fica vazio. Preencher só ao apontar para uma API
# remota (ex.: staging no Render).
VITE_API_URL=
